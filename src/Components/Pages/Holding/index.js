/* eslint-env browser */
import React, { Fragment } from 'react';
import H2Tag from '../../Atoms/H2Tag';

const Holding = () => (
  <Fragment>
    <H2Tag className="formTitle">Holding Page</H2Tag>
    <a href="/checkout">Checkout Page</a>
  </Fragment>
);

export default Holding;
